<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="canonical" href="{{ page.url | url | absoluteUrl(site.url) }}">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap" rel="stylesheet">
    {% bundledcss %}
    <title>
      {% if title !== site.name %}
        {{ title | escape }}
      {% else %}
        {{ site.name | capitalize }}
      {% endif %}
    </title>
    <meta property="og:title" content="{{ title or site.name | escape }}">
    <meta property="og:site_name" content="{{ site.name }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ site.url + page.url }}">
    {% set desc = description or content | striptags | truncate(140) %}
    {% if desc %}
      <meta name="description" content="{{ desc | escape }}">
      <meta property="og:description" content="{{ desc | escape }}">
      <meta name="description" content="{{ desc | escape }}"/>
    {% endif %}
    {# Add mermaid -- start #}
    <script src="https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js"></script>
    <script>
      mermaid.initialize({startOnLoad: true});
    </script>
    {# Add mermaid -- end #}
  </head>
  <body>
    <header class="s-grid-cols-6 m-grid-cols-12 s-gap-x-16 m-gap-x-24 l-gap-x-32 s-px-16 m-px-24 l-px-32 s-py-96 m-my-128 l-py-128">
      <h1 class="{{ "s-col-2-7 m-col-3-11 justify-self-end text-4xl medium" if title === site.name else "s-col-span-6 m-col-3-11 text-3xl medium" }}">{{ title }}</h1>
    </header>
    {{ content | safe }}
    <footer class="footer"></footer>
    {% bundledjs %}
  </body>
</html>